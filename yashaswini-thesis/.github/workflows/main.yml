name: Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up dependencies
      run: |
        echo "Setting up dependencies for serverless-fault-localisation..."
        # TODO: Inject AWS Keys later
    - name: Build and Test
      run: |
        echo "Running tests..."
        if [ -f serverless-fault-localisation/Makefile ]; then cd serverless-fault-localisation && make test || true; fi
