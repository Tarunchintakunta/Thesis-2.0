# Copy to .env and fill in. Never commit .env - it is gitignored.
# Credentials come from `aws configure` / `aws sso login`, not from this file.
STACK=faultlab
AWS_REGION=eu-west-1
API_URL=https://<api-id>.execute-api.eu-west-1.amazonaws.com/prod
ALERT_EMAIL=
