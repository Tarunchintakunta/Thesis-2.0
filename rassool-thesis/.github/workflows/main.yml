name: Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up dependencies
      run: |
        echo "Setting up dependencies for dynamodb-pk-capacity-eval..."
        # TODO: Inject AWS Keys later
    - name: Build and Test
      run: |
        echo "Running tests..."
        if [ -f dynamodb-pk-capacity-eval/Makefile ]; then cd dynamodb-pk-capacity-eval && make test || true; fi
