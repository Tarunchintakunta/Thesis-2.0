\section{Evaluation}

\textit{Note:} Mixed (W3) and Burst (W4) cells below are filled from the live DynamoDB key-cell round (\texttt{results/batches.csv}, $n{=}1$, \texttt{orders}{=}100000, \texttt{eu-west-1}, 2026-09-20). Steady (W1) and Read-heavy (W2) cells remain unfilled. Moto unit tests exercise the pipeline only and are \textbf{not} empirical findings. Confirmatory two-way ANOVA with interaction is \textbf{not identified} at $n{=}1$. Exploratory additive OLS and request-level Kruskal--Wallis are in \texttt{report/generated/hypotheses\_keycell\_n1.json} (pseudo-replication; not confirmatory). Cost figures are list-price proxies from \texttt{config/prices.yaml} (publication 2026-09-11), not Cost Explorer.

\subsection{Designed Campaign Configuration}

The live key-cell round used the following parameters (from \texttt{config/experiment.yaml} / \texttt{results/keycell\_round\_meta.txt}):

\begin{itemize}
    \item \textbf{Dataset:} 100{,}000 synthetic orders (full design allows up to 1M; Zipfian $s$ was calibrated for 1M --- caveat documented in meta)
    \item \textbf{Measured factorial slice:} K1--K3 $\times$ on-demand/provisioned $\times$ W3/W4 (\textbf{12/12} cells). W1/W2 not run. K4 remains off-factorial.
    \item \textbf{Zipfian s:} 1.070916 (hottest $\approx$10\% of keys receive $\approx$90\% of operations)
    \item \textbf{Region / pricing:} \texttt{eu-west-1} list prices in \texttt{config/prices.yaml} (not a Cost Explorer cross-check)
    \item \textbf{Teardown:} stack destroyed after the round (32 resources); evidence retained under \texttt{results/}
\end{itemize}

\subsection{Live Key-Cell Results (W3/W4)}

\subsubsection{Latency Distribution}

Table \ref{tab:sim-latency} reports measured mean and p95 latency (ms). Corresponding p99 values are in \texttt{report/generated/cell\_summary.md} / \texttt{results/keycell\_summary.csv}.

\begin{table}[h]
\centering
\caption{Latency (ms) by Configuration and Workload --- live W3/W4; W1/W2 not measured}
\label{tab:sim-latency}
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Configuration} & \textbf{Mean (W3)} & \textbf{p95 (W3)} & \textbf{Mean (W4)} & \textbf{p95 (W4)} \\ \hline
K1-OnDemand & 4.344 & 5.838 & 4.553 & 5.848 \\ \hline
K1-Provisioned & 4.364 & 5.796 & 4.890 & 6.035 \\ \hline
K2-OnDemand & 4.482 & 5.883 & 4.803 & 5.876 \\ \hline
K2-Provisioned & 4.354 & 5.765 & 4.597 & 5.848 \\ \hline
K3-OnDemand & 5.010 & 6.116 & 5.246 & 6.265 \\ \hline
K3-Provisioned & 4.895 & 5.787 & 5.356 & 6.223 \\ \hline
\end{tabular}
\end{table}

\textit{Interpretation:} Single-replicate directional evidence only. Observed p99 range on this round: 6.95--11.49\,ms. Throughput was $\approx$200 ops/s (W3) and $\approx$467 ops/s (W4) with zero measured throttles. No confirmatory $n{>}1$ distributional claim is made.

\subsubsection{Throttling Behaviour}

Table \ref{tab:sim-throttle} reports measured throttle rates. All twelve live cells recorded \texttt{throttle\_rate} $= 0$.

\begin{table}[h]
\centering
\caption{Throttle Rate (\%) by Configuration and Workload --- live W3/W4}
\label{tab:sim-throttle}
\begin{tabular}{|l|c|c|}
\hline
\textbf{Configuration} & \textbf{Throttle Rate (W3)} & \textbf{Throttle Rate (W4)} \\ \hline
K1-OnDemand & 0.0 & 0.0 \\ \hline
K1-Provisioned & 0.0 & 0.0 \\ \hline
K2-OnDemand & 0.0 & 0.0 \\ \hline
K2-Provisioned & 0.0 & 0.0 \\ \hline
K3-OnDemand & 0.0 & 0.0 \\ \hline
K3-Provisioned & 0.0 & 0.0 \\ \hline
\end{tabular}
\end{table}

\subsubsection{Cost Analysis}

Table \ref{tab:sim-cost} uses list-price costing from \texttt{config/prices.yaml} applied to measured RCU/WCU (on-demand) or provisioned capacity $\times$ wall seconds (provisioned). \textbf{No} Cost Explorer export is claimed. Values are USD per 10{,}000 successful operations.

\begin{table}[h]
\centering
\caption{Cost per 10,000 Operations (USD) --- live W3/W4 list-price proxy}
\label{tab:sim-cost}
\begin{tabular}{|l|c|c|}
\hline
\textbf{Configuration} & \textbf{Cost (W3)} & \textbf{Cost (W4)} \\ \hline
K1-OnDemand & 0.003901 & 0.003886 \\ \hline
K1-Provisioned & 0.002328 & 0.000998 \\ \hline
K2-OnDemand & 0.003874 & 0.003899 \\ \hline
K2-Provisioned & 0.010498 & 0.000998 \\ \hline
K3-OnDemand & 0.007062 & 0.007063 \\ \hline
K3-Provisioned & 0.007128 & 0.005567 \\ \hline
\end{tabular}
\end{table}

\textit{Directional only (not practitioner guidance):} On this $n{=}1$ round, K3 on-demand showed higher list-price cost per 10k than K1/K2 on-demand under both W3 and W4; provisioned W4 costs were lower than on-demand for K1/K2. Exploratory additive OLS on the six W3 cell means: key $F{=}63.6$, $p{=}0.015$ (residual df${=}2$; \textbf{not} confirmatory). Request-level Kruskal--Wallis $p$ values are $\approx 0$ from $n{=}18000$ ops/cell --- a pseudo-replication artefact, not the pre-registered batch ANOVA.

\subsection{Statistical Analysis (exploratory n=1; confirmatory ANOVA not identified)}

Two-way ANOVA with interaction is implemented in \texttt{analysis/analyse.py}. At $n{=}1$ per cell the interaction saturates residual df, so Table \ref{tab:sim-anova} reports the \textbf{exploratory additive} OLS on W3 cell means (residual df${=}2$), not the pre-registered interaction model.

\begin{table}[h]
\centering
\caption{Exploratory additive OLS for mean latency (W3, $n{=}1$ cells) --- not confirmatory}
\label{tab:sim-anova}
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Factor} & \textbf{F-statistic} & \textbf{p-value} & \textbf{Effect} \\ \hline
Key Design (K) & 63.57 & 0.0155 & exploratory, df$_{\mathrm{res}}{=}2$ \\ \hline
Capacity Mode (C) & 2.43 & 0.260 & exploratory, df$_{\mathrm{res}}{=}2$ \\ \hline
Interaction (K $\times$ C) & [NOT IDENTIFIED] & [NOT IDENTIFIED] & $n{=}1$ saturates residual \\ \hline
\end{tabular}
\end{table}

\subsection{What unit tests do and do not show}

Pytest against moto validates artefact plumbing (not CA2 empirical answers):

\begin{enumerate}
    \item Terraform modules encode six K1--K3 $\times$ mode tables; tags are project-only (no student name/ID).
    \item Workload generator Zipfian calibration and K1--K3 key schemas match design checks.
    \item Lambda handler records latency/throttle/capacity fields and counts throttles without inventing success.
    \item K3 scatter-gather path merges shard reads in unit tests.
    \item Cost helpers apply \texttt{prices.yaml} arithmetic --- \textbf{not} a live Cost Explorer cross-check.
    \item ANOVA/post-hoc code runs on fixtures labelled \texttt{data\_source = moto-smoke} / synthetic; fixture significance is \textbf{not} a DynamoDB finding.
\end{enumerate}

Moto does \textbf{not} replicate network latency, adaptive/burst capacity, auto-scaling delay, or multi-tenant effects. Optional K4 code paths are excluded from this evidence narrative.

\subsection{Residual for CA2 evidence}

The first live DynamoDB key-cell campaign (W3/W4, 12/12) is measured and the stack destroyed. Soft residuals remain: W1/W2 live cells and confirmatory repeats. Exploratory n=1 stats are filled and must not be cited as confirmatory ANOVA. Practitioner guidance stays deferred.
