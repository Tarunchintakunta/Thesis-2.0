numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.4.0
joblib>=1.3.0
pytest>=7.4.0
pytest-cov>=4.1.0
# scripts/train_lstm_and_evaluate.py --backend tensorflow fails closed.
# Optional on Python 3.10–3.12: tensorflow>=2.15
# Live AWS orchestration (SSM/S3/CW); kubectl used on the k3s node, not locally.
boto3>=1.34.0
# Optional: kubernetes>=31.0.0 (unused — live path uses kubectl on-node)
