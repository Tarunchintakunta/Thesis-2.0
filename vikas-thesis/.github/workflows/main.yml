name: Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up dependencies
      run: |
        echo "Setting up dependencies for lambda-idempotency-eval..."
        # TODO: Inject AWS Keys later
    - name: Build and Test
      run: |
        echo "Running tests..."
        if [ -f lambda-idempotency-eval/Makefile ]; then cd lambda-idempotency-eval && make test || true; fi
