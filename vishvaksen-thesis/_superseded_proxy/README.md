# Superseded proxy artefact

This directory holds the **pre-formal-CA2** War comment-ablation / TF-IDF hybrid
(`iac-security/`) plus the old SAM deploy workflow.

It is **not** the binding CA2 artefact and must **not** be used as formal
evidence for Checkov / tfsec / OPA recall.

Formal artefact: `../terraform-scanner-benchmark/`.
Binding contract: `../CA2_COMMITMENTS.md`.
