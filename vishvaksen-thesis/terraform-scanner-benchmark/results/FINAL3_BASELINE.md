# Vishvaksen final-3 baseline (local labelled-oracle)

**Date:** 2026-09-21  
**Packs:** `results/final_1/`, `final_2/`, `final_3/`  
**Protocol:** checklist + Checkov + tfsec + OPA + evaluate on N=240 labelled corpus (no AWS apply).

## Verdict
Three independent local full scans completed. CA2 re-check still **100%** (labelled-oracle method). Soft Verdet/report polish remains optional.

Artefacts per round: `summary.json`, `metrics_per_category.csv`, verdicts, `scan_times.csv`, `tool_versions.json`.
