# Tool pinning

Rule catalogues change between releases. Formal CA2 requires pinned versions.

Recorded by `scripts/pin_tools.py` into `results/tool_versions.json` at
evaluation time.

Shipped **defaults** only (no custom Checkov/tfsec policies). OPA policies
are this study’s category definitions, authored independently of module IDs.
