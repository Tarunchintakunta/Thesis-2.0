name: Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up dependencies
      run: |
        echo "Setting up dependencies for serverless-log-anomaly..."
        # TODO: Inject AWS Keys later
    - name: Build and Test
      run: |
        echo "Running tests..."
        if [ -f serverless-log-anomaly/Makefile ]; then cd serverless-log-anomaly && make test || true; fi
