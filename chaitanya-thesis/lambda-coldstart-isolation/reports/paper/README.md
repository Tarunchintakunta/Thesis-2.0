# Research report (to be written by the student)

Kondragunta Lakshmi Chaitanya - 25171216

The report itself (<= 20 pages, NCI template) is the student's own work and is
not in this repository. This page only maps each mandatory section to the
artefact outputs it can draw on.

| Section | Inputs from the artefact |
|---|---|
| 1 Abstract | final numbers from `tables/live/hypotheses.json` and `tables/live/decision_matrix.md` |
| 2 Introduction | research question and objectives (proposal); limitations in `docs/ASSUMPTIONS.md` |
| 3 Literature survey | `bib/references.bib` (26 verified entries) - rewrite, do not reuse the proposal text |
| 4 Research methodology | `docs/ANALYSIS_PLAN.md`, `configs/experiment.yaml`, classification rule (Init Duration present) |
| 5 Design and implementation | `infra/template.yaml`, `functions/`, `docs/ARCHITECTURE.md`, package manifest |
| 6 Evaluation | `tables/live/*`, `figures/live/*`; proxy results in `tables/proxy/` as supporting evidence only |
| 7 Conclusions and discussion | decision matrix, threats to validity (single region, single provider, deprecated Node.js 20 runtime, proxy vs Lambda) |
| 8 References | `bib/references.bib` |

`tables/mock/` and the mock figures are synthetic and must not appear as results.
